# Supabase project URL (Settings → API)
VITE_SUPABASE_URL=

# Publishable key (`sb_publishable_...`) — safe for browser (Settings → API Keys)
VITE_SUPABASE_PUBLISHABLE_KEY=

# Rust API base URL (no trailing slash). Local dev: http://localhost:8080
VITE_API_URL=
