// App branding shown in the UI. Mirrors iOS `AppBrand` (Core/Config/AppConfig.swift).
// Storage keys keep their legacy `incm-mgmt-` prefix so existing preferences survive the rename.
export const APP_NAME = "cash.sh";
export const APP_VERSION = "0.1.0";
export const APP_VERSION_LABEL = `${APP_NAME} v${APP_VERSION}`;
